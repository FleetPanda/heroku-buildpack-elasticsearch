#!/bin/bash

# Release script for Elasticsearch buildpack
# Returns metadata for the runtime

BUILD_DIR=$1

cat << EOF
---
default_process_types:
  elasticsearch: $BUILD_DIR/.elasticsearch/bin/elasticsearch
EOF
